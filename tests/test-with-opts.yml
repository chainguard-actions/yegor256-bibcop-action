name: Test with custom opts
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          opts: ''
      - name: Assert action completed with opts
        run: |
          echo "bibcop action ran successfully with explicit empty opts"
          echo "PASS"
